---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: relationshippolicies.knowledge.miloapis.com
spec:
  group: knowledge.miloapis.com
  names:
    categories:
    - knowledge
    kind: RelationshipPolicy
    listKind: RelationshipPolicyList
    plural: relationshippolicies
    singular: relationshippolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.relationshipType.name
      name: RelationshipType
      type: string
    - jsonPath: .spec.subject.kind
      name: Subject Kind
      type: string
    - jsonPath: .status.discoveredEdgesCount
      name: Discovered Edges
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RelationshipPolicy is the Schema for the relationshippolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RelationshipPolicySpec defines the desired state of RelationshipPolicy
            properties:
              controlPlaneContextRef:
                description: |-
                  ControlPlaneContextRef identifies the control plane context where discovered
                  ResourceRelationship objects will be created.
                properties:
                  kind:
                    description: 'Kind is the type of control plane context. Must
                      be one of: Platform, Organization, Project, User.'
                    enum:
                    - Platform
                    - Organization
                    - Project
                    - User
                    type: string
                  name:
                    description: Name is the name of the control plane context instance.
                    type: string
                required:
                - kind
                - name
                type: object
              discovery:
                description: Discovery defines how relationships are discovered from
                  subject resources.
                properties:
                  expression:
                    description: |-
                      Expression is a CEL expression that receives `subject` (the full Kubernetes object as a map)
                      and returns a list of maps with fields: apiGroup, kind, name, namespace,
                      controlPlaneContextKind, controlPlaneContextName.
                    type: string
                required:
                - expression
                type: object
              relationshipType:
                description: RelationshipType references the RelationshipType that
                  discovered edges will be of.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              subject:
                description: Subject defines which resources this policy watches and
                  evaluates.
                properties:
                  apiGroup:
                    description: APIGroup is the API group of the subject resources
                      to watch.
                    type: string
                  kind:
                    description: Kind is the kind of the subject resources to watch.
                    type: string
                  namespaces:
                    description: Namespaces is the list of namespaces to watch. If
                      empty, all namespaces are watched.
                    items:
                      type: string
                    type: array
                required:
                - apiGroup
                - kind
                type: object
            required:
            - controlPlaneContextRef
            - discovery
            - relationshipType
            - subject
            type: object
          status:
            description: RelationshipPolicyStatus defines the observed state of RelationshipPolicy
            properties:
              conditions:
                description: |-
                  Conditions represents the observations of a relationship policy's current state.
                  Known condition types are: "Ready"
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              discoveredEdgesCount:
                description: DiscoveredEdgesCount is the total number of ResourceRelationship
                  objects currently managed by this policy.
                format: int64
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this RelationshipPolicy by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
